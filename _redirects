# Cloudflare Pages serves /about from about.html natively and 308s
# /about.html -> /about. Do NOT add clean-URL rewrite rules here: a rule
# like "/about /about.html 200" loops against that canonicalisation.
# Only non-1:1 paths belong in this file.
/malaysia-digital-marketing-statistics-2026 /statistics_pages/malaysia-digital-marketing-statistics-2026 200

# Host-level redirects (www -> apex, pages.dev -> apex) are NOT honoured by
# Pages _redirects (tested 15 Sep 2026). Use a Cloudflare Redirect Rule in the
# zone dashboard instead: Rules -> Redirect Rules -> "Redirect from WWW to root".
